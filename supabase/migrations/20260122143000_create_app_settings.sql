-- 20260122143000_create_app_settings.sql (placeholder)
-- No-op placeholder. The CREATE TABLE was removed from local and consolidated into the baseline (dropped),
-- but remote history references this version; keep placeholder to allow db push to proceed.

BEGIN;
-- no-op
SELECT 1;
COMMIT;
